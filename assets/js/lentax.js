/* =========================================================
   LENTAX SHARED JS
   Safe to load on every embed
   ========================================================= */

(function () {
  // Placeholder for shared helpers later.
  // Intentionally minimal to avoid side effects.

  window.Lentax = window.Lentax || {
    version: "1.0.0"
  };
})();
